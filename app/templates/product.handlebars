<div class='row'>
  <img {{bind-attr src=image}} class='col-lg-8' />
  <div class='col-lg-4'>
    <h2>{{ name }}</h2>
    <p>{{ description }}</p>
    <span class='money'>{{ cents }}</span>
    <button {{action "addToCart" this }} class='btn btn-success'>Add To Cart</button>
  </div>
</div>